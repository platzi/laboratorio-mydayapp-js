/**
 * Enum for keys code.
 * @readonly
 * @enum {string}
 */
export const Keys = {
	Enter: 'Enter',
	NumpadEnter: 'NumpadEnter',
	Escape: 'Escape',
};

Object.freeze(Keys);

/**
 * Enum for keys code.
 * @readonly
 * @enum {string}
 */
export const Operation = {
	Plus: 'Plus',
	Minus: 'Minus',
};

Object.freeze(Operation);
